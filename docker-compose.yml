version: '3'

services:
  authms:
    build: 
      context: ./
      dockerfile: AuthDockerfile
    command: ./authms
    ports:
      - 8082:8082
    depends_on:
      redis:
        condition: service_healthy
    links:
      - redis 

  select:
    build: 
      context: ./
      dockerfile: SelectDockerfile
    command: ./selectms
    ports:
      - 8083:8083

  apiserver:
    build: 
      context: ./
      dockerfile: Dockerfile
    command: ./apiserver
    ports:
      - 8080:8080
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    links:
      - redis

  surveyms:
    build: 
      context: ./
      dockerfile: SurveyDockerfile
    command: ./surveyms
    ports:
      - 8084:8084
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    links:
      - redis 

  db:
    restart: always
    image: postgres:latest
    volumes:
      - ./migrations/init.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "zxc123"
      POSTGRES_DB: "adhub"
    ports:
      - 5436:5432
    healthcheck:
      test: ["CMD", "pg_isready", "-q", "-d", "adhub", "-U", "postgres"]
      interval: 5s
      retries: 10

  redis:
    image: redis:latest
    environment:
      - REDIS_DB=0
    ports:
      - 6379:6379
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 2s
      retries: 3

  survey_db:
    restart: always
    image: postgres:latest
    volumes:
      - ./survey/migrations/init.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
      POSTGRES_PORT: 5434
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "zxc123"
      POSTGRES_DB: "survey"
    ports:
      - 5434:5434
    healthcheck:
      test: ["CMD", "pg_isready", "-q", "-d", "survey", "-U", "postgres"]
      interval: 5s
      retries: 10

